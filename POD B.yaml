apiVersion: v1
kind: Pod
metadata:
  name: POD B
spec:
  containers:
  - name: POD B-123
    image: redis
    resources:
      limits:
        memory: "400Mb"
        cpu: "100m"
      requests:
        memory: "200Mb"
        cpu: "50m"
